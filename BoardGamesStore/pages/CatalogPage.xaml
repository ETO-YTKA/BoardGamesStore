<Page x:Class="BoardGamesStore.pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BoardGamesStore.pages"
      xmlns:bgstore="clr-namespace:BoardGamesStore.db"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CatalogPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="/image/logo.png" Height="40"/>
            <TextBlock Text="Каталог" FontSize="35" HorizontalAlignment="Center"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,10" Grid.Row="1">
            <ListView Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type bgstore:Games}">
                        <Border BorderBrush="Pink" BorderThickness="10" Padding="10" CornerRadius="10">
                            <StackPanel>
                                <Image Source="{Binding ImagePath}"/>
                                <TextBlock Text="{Binding Title}"/>
                                <TextBlock Text="{Binding AgeRestriction}"/>
                                <TextBlock Text="{Binding MinPlayers}"/>
                                <TextBlock Text="{Binding MaxPlayers}"/>
                                <TextBlock Text="{Binding PlayTime}"/>
                                <TextBlock Text="{Binding Price}"/>
                                <TextBlock Text="{Binding GenreGame.Name}"/>
                                <TextBlock Text="{Binding DesignerGames.Name}"/>
                                <TextBlock Text="{Binding PublisherGames.Name}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
